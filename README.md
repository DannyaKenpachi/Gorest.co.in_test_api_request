# Gorest.co.in_test_api_request
API Tests (Python + Requests)
Проект с автотестами для проверки REST API сервиса GoRest.co.in.
Написан на Python с использованием библиотеки requests и фреймворка pytest.

🛠 Стек технологий
Python 3 — язык программирования.
Requests — отправка HTTP-запросов (GET, POST, PUT, DELETE).
Pytest — запуск тестов и фикстуры.
Allure — генерация красивых отчетов.
Random — генерация тестовых данных.

📂 Структура проекта
tests/ — папка с тестами.
tests/conftest.py — фикстуры (настройка клиента, создание/удаление тестового юзера).
requirements.txt — список библиотек.
.github/workflows/ — настройка CI/CD (автозапуск в GitHub Actions).

🚀 Как запустить локально
1. Клонировать репозиторий:
git clone https://github.com/Dannyakenpachi/gorest_api_tests.git
cd gorest_api_tests
2. Установить зависимости:
pip install -r requirements.txt
3. Запустить тесты:
pytest tests/ --alluredir=allure-results
4. Посмотреть отчет Allure:
allure serve allure-results

✅ Что проверяют тесты
GET /users — получение списка пользователей.
POST /users — создание нового пользователя (через фикстуру conftest.py).
PATCH /users/{id} — обновление данных созданного пользователя.
DELETE /users/{id} — удаление пользователя (автоматически после тестов).
